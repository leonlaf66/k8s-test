# AWS Credentials Secret (可选 - 用于 S3 Sink)
# 实际使用时替换 base64 编码的值，或使用 IRSA
apiVersion: v1
kind: Secret
metadata:
  name: aws-credentials
  namespace: kafka-connect
type: Opaque
data:
  # echo -n "YOUR_ACCESS_KEY" | base64
  access-key-id: QUNDRVNTX0tFWQ==
  # echo -n "YOUR_SECRET_KEY" | base64  
  secret-access-key: U0VDUkVUX0tFWQ==
